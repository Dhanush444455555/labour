{
  "name": "labour-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others \"npm run --prefix backend dev\" \"npm run --prefix frontend dev\"",
    "start": "concurrently --kill-others \"npm run --prefix backend start\" \"npm run --prefix frontend dev\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
